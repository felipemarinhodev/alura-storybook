import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs/blocks";
import Button from "./";

<Meta title="Components/Button" component={Button} />

# Button

Aqui vai a descrição do componente.

<ArgsTable of={Button} />

## Button Primary

<Canvas>
	<Story
		name="Button Primary"
		args={{ children: "Texto do botão", variant: "primary" }}
	>
		{(args) => <Button {...args} />}
	</Story>
</Canvas>

## Button Accent

<Canvas>
	<Story
		name="Button Accent"
		args={{ children: "Texto do botão 2", variant: "accent" }}
	>
		{(args) => <Button {...args} />}
	</Story>
</Canvas>
